import React from 'react'
import './header.sass'

const Header = () => {
    return (
        <header>
            <a href="">Log in</a>
            <a href="">Register</a>
        </header>
    );
};

export default Header;